using Microsoft.Xna.Framework;

namespace RType.Vehicle;

public readonly record struct ShadowSuspensionCornerState(
    bool HasContact,
    string MissReason,
    string SourceName,
    int TriangleIndex,
    Vector3 HardpointWorld,
    Vector3 SuspensionAxisWorld,
    Vector3 ContactPoint,
    Vector3 ContactNormal,
    Vector3 SolvedWheelCenter,
    Vector3 CalculatedTyreContactPoint,
    float SuspensionLengthMeters,
    float StaticReferenceLengthMeters,
    float EquivalentFreeLengthMeters,
    float StaticPreloadForceN,
    float CompressionMeters,
    float CompressionVelocityMetersPerSecond,
    float DynamicSpringForceN,
    float BumpStopForceN,
    float SpringForceN,
    float DamperForceN,
    float ArbContributionN,
    float UnclampedSupportForceN,
    float SupportClampErrorN,
    float SupportForceN,
    float TunedNormalLoadN,
    float BumpTravelRemainingMeters,
    float DroopTravelRemainingMeters,
    float WheelCenterAxisErrorMeters,
    float TyrePlaneDistanceErrorMeters,
    float AxisNormalAngleDegrees,
    int CandidateTriangleCount,
    string ContactState,
    bool AntiTunnelRecovered,
    float AntiTunnelPenetrationMeters,
    bool BumpLimitHit,
    bool DroopLimitHit);
